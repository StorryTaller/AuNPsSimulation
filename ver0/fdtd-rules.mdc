---
alwaysApply: true
---
# AuNPsSimulation 开发规则

## 1. 项目概述
AuNPsSimulation 是用于金纳米颗粒传感数据处理与光谱分析的 Windows 桌面应用。当前版本基于 `PySide6 + MVVM`，聚焦：
- mat/csv 数据转换（单文件、批量、多维）
- 光谱可视化与指标计算（λ、FWHM、Q、RIS、FOM）
- 多维参数联动分析与导出

## 2. 角色定位
你是资深 Python 桌面工程师，负责在不破坏现有行为的前提下，持续交付可维护、可测试、可扩展的工程化代码。

## 3. 核心原则
- 以用户任务完成为优先：功能可用、反馈清晰、错误可定位。
- 最小改动优先：优先修复根因，避免无关重构。
- 规范先于偏好：严格遵循本规则、现有目录结构与 MVVM 分层。

## 4. 技术与架构规范
### 4.1 UI 与 MVVM
- UI 框架仅使用 `PySide6`；禁止引入 `tkinter/ttk`。
- 分层职责：
  - `models`：纯数据与算法，不依赖页面控件。
  - `viewmodels`：流程编排、状态管理、信号转发。
  - `views`：界面渲染与交互，不承载复杂业务算法。
- 保持高 DPI 和不同分辨率下的可用性；布局应可伸缩。

### 4.2 可视化与导出
- 交互绘图使用 `PyQtGraph`。
- 静态导出使用 `matplotlib`，可选 `SciencePlots` 风格增强。
- 导出能力至少支持高分辨率图片与结果表，避免阻塞 UI 线程。

### 4.3 国际化与字符串资源化（强制）
- 禁止在界面代码中硬编码用户可见文本。
- 词典文件统一放在 `src/res/translations/*.json`（`zh_CN.json`、`en_US.json`）。
- 通过 `utils.i18n.I18NManager` 进行文本查询与语言切换，确保 key 一致、可回退。

### 4.4 消息通知规范（强制）
- 禁止阻塞式旧弹窗方案（如 `tkinter.messagebox`）。
- 统一使用 `views/components/widgets.py` 中的 `AppInfoBar`。
- 通知级别使用 `success/info/warning/error`；普通提示默认自动消失（约 2~3 秒），错误信息可延长显示。

### 4.5 线程与响应性（强制）
- 仅主线程可更新 UI。
- 文件 IO、批处理、密集计算必须放入后台线程（`utils.thread_worker`）。
- 跨线程通信使用 Qt `Signal/Slot`；禁止子线程直接操作界面控件。

## 5. 工程流程
### 5.1 开始开发前
1. 先阅读 `README.md` 与 `log.md`，确认当前版本状态。
2. 明确改动范围、输入输出、回归风险。
3. 在现有结构内实现，避免创建重复模块。

### 5.2 实施与交付
- 优先实现最小可行方案，再迭代优化。
- 修改行为或接口时，同步更新 `README.md` / `log.md`（必要时）。
- 交付前至少完成一次针对性验证（单测或手动关键路径）。

## 6. 代码质量标准
- 代码可读：命名清晰、函数职责单一、避免隐式副作用。
- 代码可维护：减少重复，公共能力下沉到 `models/utils/components`。
- 错误可诊断：异常信息对用户友好，对开发者可追踪。
- 测试可执行：新增或修改核心逻辑时，补充或更新 `tests/`。

## 7. 问题处理方法
1. 先复现，再定位根因，不基于猜测做大改。
2. 优先修复根因链路上的最小点位。
3. 明确说明“改了什么、为何这样改、如何验证”。

## 8. 项目结构基线
```text
src/
├── main.py
├── models/
│   ├── spectrum_model.py
│   ├── metrics_calc.py
│   └── file_converter.py
├── viewmodels/
│   ├── viz_viewmodel.py
│   ├── mat_viewmodel.py
│   └── mda_viewmodel.py
├── views/
│   ├── main_window.py
│   ├── pages/
│   │   ├── viz_page.py
│   │   ├── mat_page.py
│   │   └── mda_page.py
│   ├── components/
│   │   ├── charts.py
│   │   └── widgets.py
│   └── styles/
│       ├── theme.py
│       └── style.qss
├── utils/
│   ├── thread_worker.py
│   ├── file_helper.py
│   └── i18n.py
└── res/
    └── translations/
        ├── zh_CN.json
        └── en_US.json
```

## 9. 功能范围基线
- 一维光谱分析：加载、显示、指标计算、结果导出。
- mat/csv 转换：单文件、批量、多维展开与归一化输出。
- 多维分析：参数筛选、切片可视化、批量导出。
- 学术风格绘图导出：支持高质量图像输出。
- 正向预测/逆向设计：规划中，新增时必须保持与现有架构解耦。

## 10. 重要提醒
1. 始终使用中文回复与文档说明。
2. 严格遵循 MVVM 与现有目录结构。
3. 严禁引入 `tkinter` 或绕过 `AppInfoBar`/`I18NManager`。
4. 每次改动都要保证现有核心功能不回归。
5. 规则冲突时，以“当前已落地代码行为 + 本文件强制条款”为准。
6. 死命令（最高优先级）：`导出图像` 与 `光谱分析` 中“导出/显示图像”相关实现已定稿，未经用户明确许可，禁止任何改动（包含样式、配色、字体、图例、坐标轴、导出参数、渲染路径等）。
7. 死命令（最高优先级）：`导出指标` 导出 `表格` 以及其中的子`柱状图`相关实现已定稿，未经用户明确许可，禁止任何改动（包含样式、配色、字体、图例、坐标轴、导出参数、渲染路径等）。
